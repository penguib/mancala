local bot = {}

bot.settings = {
    level = 5, -- max
}

function bot.move(board)
    -- Top moves
    -- 1) Get last pebble to land into bank while closest to bank
    -- 2) Get pebble into bank
    -- 3) Random move
    --

    local data = board.data
end

return bot
